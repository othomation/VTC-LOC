{% extends 'base.html.twig' %}

{% block title %}Vehicule
{% endblock %}

{% block body %}
	<h1>Vehicule</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ vehicule.id }}</td>
			</tr>
			<tr>
				<th>Marque</th>
				<td>{{ vehicule.marque }}</td>
			</tr>
			<tr>
				<th>Modele</th>
				<td>{{ vehicule.modele }}</td>
			</tr>
			<tr>
				<th>Couleur</th>
				<td>{{ vehicule.couleur }}</td>
			</tr>
			<tr>
				<th>Immatriculation</th>
				<td>{{ vehicule.immatriculation }}</td>
			</tr>
			<tr>
				<th>Conducteurs
				</th>
				<td>
					<ul>
						{% if vehicule.getConduteur() is empty %}
							<p>Pas de location en cours...</p>
						{% else %}
							{% for conducteur in vehicule.getConduteur() %}
								<li>
									{{conduteur.getPrenom()}}
								</li>
							{% endfor %}
						{% endif %}
					</ul>
				</td>
			</tr>
		</tbody>
	</td>
</tr></tbody></table><div><a href="{{ path('vehicule_index') }}" class="btn btn-primary">back to list</a><a href="{{ path('vehicule_edit', {'id': vehicule.id}) }}" class="btn btn-warning">edit</a>{{ include('vehicule/_delete_form.html.twig') }}</div>{% endblock %}
