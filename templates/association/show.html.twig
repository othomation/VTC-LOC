{% extends 'base.html.twig' %}

{% block title %}Association
{% endblock %}

{% block body %}
	<h1>Association</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ association.id }}</td>
			</tr>
			<tr>
				<th>Conducteur</th>
				<td>{{ association.conducteur }}</td>
			</tr>
			<tr>
				<th>vehicule</th>
				<td>{{ association.vehicule }}</td>
			</tr>

		</tbody>
	</table>

<a href="{{ path('association_index') }}" class="btn btn-primary">back to list</a>


<a href="{{ path('association_edit', {'id': association.id}) }}" class="btn btn-warning">edit</a>


	{{ include('association/_delete_form.html.twig') }}
{% endblock %}
